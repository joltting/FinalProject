<%= form_for(@team_list) do |f| %>
  <% if @team_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@team_list.errors.count, "error") %> prohibited this team_list from being saved:</h2>

      <ul>
      <% @team_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
